<!DOCTYPE html>
<html>
    <head>
        <title>Products</title>
        <meta charset="utf-8">
    </head>
    <body>
        <form action="/addToCart" method="post">
            <div id="container">
                <div>
                    <% for (let i of prod) { %>
                        <li><a href="/product?id=<%=i.id%>"><%= i.name %></a></li>
                    <% } %>
                </div>
            </div>
        </form>
    </body>
</html>